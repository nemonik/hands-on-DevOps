# Drone

Drone is a Continuous Delivery system built on container technology. 

Spin up via

```
docker-compose build
docker-compose up
```

Then access via

[http://{{ ipaddr }}](http://{{ ipaddr }})

To stop:

```
docker-compose stop
```

To remove:

```
docker-compose rm
```
